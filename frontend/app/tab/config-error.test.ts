import { describe, expect, it } from "vitest";
import { ConfigErrorIcon } from "./config-error";

describe("ConfigErrorIcon", () => {
    it("should be exported as a named export", () => {
        expect(ConfigErrorIcon).toBeDefined();
    });
});
